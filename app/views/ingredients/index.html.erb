<h1>Ingredients</h1>

<% @ingredients.each do |ingredient| %>
    <p><%= link_to ingredient.name, ingredient_path(ingredient.id) %>: <%= ingredient.allergies.count %> reported allergies.</p><br>
<% end %>